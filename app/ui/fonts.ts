import { Inter, Lusitana, Lacquer } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
})

export const lacquer = Lacquer({
  weight: ['400'],
  subsets: ['latin'],
})
